// Enum separado que deberías crear en el mismo paquete
package com.brunovelasquez.accountservice.model;

public enum TransactionType {
    DEPOSIT,    // Representa ingreso de dinero a la cuenta
    WITHDRAWAL  // Representa salida de dinero desde la cuenta
}
